# Render Blueprint configuration to deploy Database and WebSocket server together
services:
  - type: web
    name: rtxnotion-ws
    runtime: node
    buildCommand: npm install --legacy-peer-deps && npx prisma generate
    startCommand: npx tsx server/ws-server.ts
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rtxnotion-db
          property: connectionString
      - key: WS_PORT
        value: 10000
      - key: NODE_ENV
        value: production

databases:
  - name: rtxnotion-db
    databaseName: rtxnotion
    user: rtxnotion_user
    plan: free
